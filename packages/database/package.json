{
  "name": "@serenity/database",
  "version": "0.1.0",
  "description": "Database layer and schema for Serenity Notes",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/**/*.ts",
    "migrate": "node dist/migrations/run.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@serenity/core": "*",
    "pg": "^8.11.3",
    "pg-promise": "^11.5.4",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/pg": "^8.10.2",
    "typescript": "^5.2.2"
  }
}