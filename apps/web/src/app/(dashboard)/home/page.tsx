'use client'

export const dynamic = 'force-dynamic'

/**
 * Next.js wrapper for shared HomePage component
 * The actual component logic is in @serenity/ui/pages
 */
import { HomePage } from '@serenity/ui/pages'

export default HomePage
